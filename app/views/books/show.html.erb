<div class="container px-5 px-sm-0">
  <%= render 'shared/error_messages', model: @book %>
  <div class="row">
    <div class="col-md-3">
      <%= render 'users/userinfo', user: @user %>
      <%= render 'shared/post_form', book: @book_new, user: @user %>
    </div>
    <div class="col-md-8 offset-md-1">
      <%= render 'shared/book_detail', book: @book, user: @user %>
    </div>
  </div>
</div>